/**
 * 
 */
alert('hello webpack!'); 
/*window.onload = () => {
	const header = document.querySelector('header');
	header.querySelectorAll('a').forEach((e, k, p) => {
		e.addEventListener('click', (event) => {
			event.preventDefault();
			console.log(e.href);
		});
	});
}*/